#include "bits/stdc++.h"
using namespace std;
#define REP(i, a, b) for (int i = (int)(a); i < (int)(b); i++)
#define rep(i, a) REP(i, 0, a)


int main(void) {
    int T;
    cin >> T;
    while (T--) {

        printf("Case %d: %d", loop, ans);
    }

    return 0;
}
